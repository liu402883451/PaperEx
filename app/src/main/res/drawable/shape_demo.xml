<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" 
    android:shape="rectangle">
    <!-- 如果shape属性没有指定，那么就默认是rectangle -->
     <!-- 边框 -->
     <stroke
         android:width="1dp"
         android:color="#FFFFFF"
         android:dashWidth="10dp"
         android:dashGap="0dp"
         />
</shape>
